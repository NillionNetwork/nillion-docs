import FeatureSection from '@site/src/components/Features/FeatureSection/index';
import { apiCards } from '@site/src/utils/apiCards';

# Overview

Each of the [Blind Modules](/blind-computer/learn/blind-modules) is accompanied by its own dedicated API. Once you obtain [API Access](/blind-computer/build/storage/api-access) for nilDB or access via [nilai.nillion.com](https://nilai.nillion.com) for nilAI, you can begin interacting directly via one or more of the below APIs.

<FeatureSection data={apiCards} />
